# Specialist role cards

These files are intentionally lightweight.

If the host supports true subagents, use the relevant file as the specialist's instruction set. If it does not, the main agent should read the relevant role card and apply it in the current context.

Choose one primary role for most tasks. Add at most one reviewer role for complex work unless more are clearly needed.
